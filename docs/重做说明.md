# 为什么重做

旧项目同时叠加了 WebSocket、MCP、UI 重构和多轮临时补丁，定位问题变得困难。Clean 版把链路拆开：

1. 网页遥控器 -> REST 服务端
2. Android 每 1.5 秒领取命令
3. Android 调用系统媒体控制
4. Android 回传 received / executed / failed
5. MCP 只是另一种发命令入口

因此即使某个 AI 客户端或 MCP 入口出错，也能先用网页判断 Android 与 QQ 音乐是否工作。
