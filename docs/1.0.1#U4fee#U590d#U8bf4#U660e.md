# 1.0.1 构建修复

- Android 端由 Kotlin 改为纯 Java，移除 `compileDebugKotlin` 失败路径。
- 服务端 `package-lock.json` 的下载地址统一改为公开 npm registry。
- GitHub Actions 固定 npm 10.8.2，并显式使用 `https://registry.npmjs.org/`。
- Android 构建仍使用 Gradle 8.10.2、AGP 8.8.2、JDK 17 与 Android 35。
- 服务端本地 TypeScript 编译与 2 项自动测试通过。
